# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository is in its initial state. No source code, build system, package manager, or test framework has been set up yet. The `.gitignore` is pre-configured for a Node.js/JavaScript project (npm, yarn, pnpm, Next.js, Nuxt, SvelteKit, etc.).

Once the project stack is established, update this file with:
- Build, lint, and test commands
- How to run a single test
- Architecture overview and key design decisions
